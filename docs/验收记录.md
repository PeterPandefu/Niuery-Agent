# 阶段验收记录

日期：2026-09-12。执行环境：Windows、.NET SDK 10.0.203、Node.js 24.14.1。

## 总览

| 阶段 | 状态 | 证据或阻塞 |
| --- | --- | --- |
| 1：架构与 MAF | 通过 | 用户改用 gpt-4.1 后真实工具调用、流式输出和会话恢复通过 |
| 2：桌面与 Worker | 实施与验收中 | Worker 真实协议、取消、崩溃恢复已通过；正在完成桌面验证 |
| 3：编码闭环 | 未开始 | 等待前序阶段 |
| 4：可靠性 | 通过（确定性范围） | 23 项安全/策略单测通过；真实模型触发越界工具的脚本未作为通过依据 |
| 5：多模型与评测 | 通过（双模型） | Routin gpt-4.1 与 gpt-4o-mini 真实请求通过；Ollama 按用户要求跳过 |

## 阶段 1 已完成

- 首版需求、架构决策、通信协议草案。
- .NET 解决方案、MAF 运行时适配项目、诊断 CLI、xUnit 测试项目。
- 固定 Microsoft.Agents.AI.Harness 1.21.0，生成 NuGet 锁文件。
- OpenAI 兼容与 Ollama 的配置类型；本地敏感配置被 Git 忽略。
- API Key 仅从服务配置读取，不写入任务、事件或日志。
- 真实模型探针：工具返回随机证据、流式文本、会话序列化与恢复后的记忆核对。
- 单命令验收脚本，配置缺失、失败与取消均返回非零状态。

## 执行结果

1. `dotnet restore Niuery.Agent.slnx --locked-mode`：通过。
2. `dotnet build Niuery.Agent.slnx --no-restore`：通过，0 警告、0 错误。
3. `dotnet test Niuery.Agent.slnx --no-build --logger 'trx;LogFileName=stage1.trx'`：15 项通过，0 失败，0 跳过。
4. 示例配置探针：正确拒绝占位模型，输出 `probe.blocked`。
5. 真实 Routin 探针：输出 `probe.failed`、HTTP 500。

离线测试覆盖真实 MAF 工具循环、流式输出、会话恢复不重复执行工具、执行中的取消、未审批不执行、审批状态恢复后的批准与拒绝、模型配置和凭证缺失。模型响应由测试专用脚本客户端产生；这些测试不证明真实模型能力。

## 真实服务诊断

用户指定接口：`https://api.routin.ai/v1`。

用户指定模型：`deepseek-v4.1-flash-expires-on-0910`。

API Key 已配置在本机服务配置中，连接请求成功，未输出到日志或事件。

`GET /v1/models` 成功，共返回 94 个模型；未找到指定模型。绕过 MAF，直接请求同一模型的基础文本 Chat Completions，也返回 HTTP 500。服务的错误含义为：所有分组中均没有该模型可用的 AI 提供商。

结论：当前阻塞在指定模型的服务可用性，不能用离线结果宣称真实验收完成，也不擅自替换用户指定模型。待用户提供该接口下可用模型名或恢复原模型后，运行真实探针，再进入阶段 2。

## 尚未完成

真实模型流式工具调用及恢复验收、桌面工作台、生产 Worker、SQLite、编码工具、安全策略与真实任务评测尚未验收。阶段 1 审批测试是 MAF 接入验证，不代表生产权限与沙箱已实现。

## 第 3～5 阶段结果

- 第 3 阶段真实编码闭环通过：模型读取 `Calculator.cs`，通过真实审批事件申请补丁，批准后实际写入 `a + b`，并生成 `artifact.created` 差异制品。报告：`artifacts/stage3.json`。
- 第 4 阶段 23 项单测通过，包含路径越界、秘密目录、符号链接、补丁哈希保护、命令取消、日志凭证脱敏和预算限制。报告：`artifacts/stage4.json`（若脚本未完成则不以模型行为推断替代策略测试）。
- 第 5 阶段双模型真实评测通过：`gpt-4.1` 52.9 秒、`gpt-4o-mini` 6.7 秒，均完成真实 Worker 执行并返回预期中文验收文本。报告：`artifacts/stage5.json`。

## 生产体验 v0.2 验收（2026-09-12）

计划交付：补丁撤销的哈希保护、Git worktree 创建/删除、30 条可复现评测任务、全量回归。

- Worktree 与制品撤销服务已实现，并纳入 Worker 协议（`worktree.create`、`worktree.remove`、`artifact.undo`）。
- `evals/tasks.json` 包含 30 条唯一任务，覆盖 bugfix、test、refactor、feature、compile、docs 六类；`node scripts/validate-evals.mjs` 通过。
- .NET 回归测试：23 项通过；协议生成一致性通过；.NET 构建通过。
- Electron 构建通过；npm 审计 0 个漏洞。
- Electron 真实冒烟复测通过：真实窗口启动、选择工作区、发送任务、等待 `run.completed`、显示模型结果和活动视图均通过；此前超时判定为一次模型服务长尾，复测未重现。
- 30 条评测任务已完成结构验收；尚未逐条运行 30 个真实编码任务，因此不把“30 条评测集”写成 30/30 任务质量通过。

生产体验 v0.2 结论：实现和冒烟验收通过；真实评测集已就绪，批量运行结果待后续单独执行。

## 单步状态机验收（2026-09-13）

新增 `workspace.read`、`workspace.apply` 两个协议操作，将读取、哈希校验、审批、写入和撤销拆成可恢复步骤。`node scripts/verify-state-machine.mjs` 通过：真实文件读取、`expectedHash`、审批事件与响应、实际写入、差异制品和撤销均通过。报告：`artifacts/state-machine.json`。

## 5 个真实隔离代码任务验收（2026-09-13）

执行了 add、multiply、greeting、join、clamp 五个隔离 C# 文件任务。每项都完成 `workspace.read → workspace.apply → approval.respond → artifact.undo`，并检查实际文件内容前后变化。

- 完成：5/5。
- 通过：5/5，成功率 100%。
- 每项均产生审批事件、实际修改和撤销结果，平均耗时约 180 毫秒。
- 报告：`artifacts/evals-state-quality.json`。

该结果验证的是工具状态机和文件修改安全边界，不代表模型自主规划、测试修复或复杂多文件编码质量。下一步应将同一状态机接入模型产出的结构化补丁计划，并在真实样例仓库中运行测试命令。

## 测试命令状态机验收（2026-09-13）

新增 `workspace.command`，将命令审批、启动、退出码和输出记录拆为异步事件。`node scripts/verify-command-state.mjs` 使用真实 `dotnet --version` 通过：审批请求、批准、命令完成、退出码 0 和版本输出均已核对。报告：`artifacts/command-state.json`。

## 真实 dotnet test 闭环验收（2026-09-13）

创建隔离 C# 项目，先执行失败测试并确认非零退出码；随后读取文件、批准补丁、实际写入 `a + b`，再次批准并执行 `dotnet test`。最终退出码为 0，报告：`artifacts/dotnet-test-loop.json`。

首次脚本发现审批事件游标复用问题，已改为按 `runId + sequence + approvalId + digest` 关联。修复后的闭环验收通过：初始 `dotnet test` 退出码 1，应用补丁并批准后再次测试退出码 0。

## 多文件测试闭环验收（2026-09-13）

在隔离 C# 项目中修改两个文件，每个文件分别读取并校验哈希、请求审批、写入补丁和生成制品；随后执行真实 `dotnet test`。修复前退出码为 1，两个文件修改后退出码为 0，报告：`artifacts/multifile-test-loop.json`。

## 30 条基线批量评测（2026-09-12）

执行计划：五路受控并发、每条任务 30 秒超时、每批增量写入报告，使用 Routin `gpt-4o-mini` 和真实 Worker。

- 完成：30/30。
- 通过：15/30。
- 失败：15/30。
- 观测成功率：50%。
- 报告：`artifacts/evals-baseline.json`。

本轮使用五路受控并发和单任务 90 秒终态窗口，并新增 `run.started`、首个输出时间、终态、事件数和错误字段。结果为 19 条通过、11 条失败，成功率 63.33%。其中 7 条为终态完成但输出未包含严格验收短语，4 条为 90 秒内未收到终态；报告可区分两类情况。它测量的是协议任务完成率，不等同于逐条真实代码修改质量，也不能据此宣称模型质量为 63.33%。

## 后续进展：用户指定 gpt-4.1

用户授权改用 `gpt-4.1`。真实探针通过：1 次工具调用、31 个文本增量、会话恢复后保留随机验证字符串，耗时 60543 毫秒。上文原模型阻塞属于历史记录，已解除。

第 2 阶段 Worker 验收脚本已通过协议握手、真实模型流式输出、任务取消、进程退出后中断标记、SQLite 历史恢复及事件序号检查。报告位于 `artifacts/stage2.json`。

依赖升级至 SQLitePCLRaw.bundle_e_sqlite3 3.0.5、Electron 44.3.0、Vite 6.4.3，解决首次安装发现的安全公告；npm 当前审计为 0 漏洞。桌面构建通过。
